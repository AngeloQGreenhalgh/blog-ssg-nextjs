# Variáveis de ambiente
# Simula demora nas consultas de base de dados
SIMULATE_WAIT_IN_MS=0

# Tamanho máximo para upload de imagens em bytes
# Uso de (NEXT_PUBLIC_) indica que a variável ficará no lado do cliente
NEXT_PUBLIC_IMAGE_UPLOAD_MAX_SIZE=12345

# Nome da pasta aonde as imagens são salvas após upload
IMAGE_UPLOAD_DIRECTORY='pasta_de_upload'

# URL completa do servidor de imagens sem barra final
IMAGE_SERVER_URL='http://example/pasta-upload'
